#include <iostream>
using namespace std;

void Pyramid(int pointer, int num);
void Pyramid2(int num);
int main()
{
	int num;
	int chooise;
	while (true)
	{
		cout << "Enter word (1) or (0): ";
		cin >> chooise;

		if (!cin)
		{
			cin.clear();
			cout << "Enter correct number\n";
			while (cin.get() != '\n')
				continue;

			continue;
		}
		
		if (chooise == 0 || chooise == 1)
			break;
	}

	do
	{
		cout << "Enter num: ";
		cin >> num;

		if (!cin)
		{
			cin.clear();
			cout << "Enter correct number\n";
			while (cin.get() != '\n')
				continue;

			continue;
		}

	} while (num <= 0);
	Pyramid(chooise, num);
	system("pause");
	return 0;
}

void Pyramid(int pointer, int num)
{
	if (pointer == 1)
		for (int i = 0; i <= num; i++)
		{
			for (int k = num - i; k > 0; k--)
				cout << " ";
			for (int j = i; j > 0; j--)
				cout << "*";

			cout << " ";
			for (int j = i; j > 0; j--)
				cout << "*";
			cout << endl;
		}
	else
		Pyeamid2num);

	cout << endl;
}
